# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Django
*.log
db.sqlite3
/backend/media/
/backend/staticfiles/
/backend/.env

# Environment / secrets
.env
.env.local

# Testing
.pytest_cache/
.coverage
htmlcov/

# Flutter
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/build/
mobile/.packages
# pubspec.lock IS committed: this is an application, not a library, so CI must
# build the exact dependency set that was tested. Leaving it out let a
# transitive bump (path_provider_android 2.3.x -> jni) break the APK build on
# a day when nothing in this repo had changed.

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

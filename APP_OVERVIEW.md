# English Master — وصف شامل للتطبيق

منصّة تعليمية متكاملة (LMS) لتعلّم اللغة الإنجليزية موجَّهة للناطقين بالعربية،
مبنية بمعمارية **Django REST Framework** (الخادم) + **Flutter** (تطبيق الجوّال)،
وتغطّي المسار الكامل من التسجيل إلى الشهادة، مروراً بتجربة تعلّم إتقاني (Mastery
Learning) متعدّدة المراحل مستوحاة من أفضل ممارسات تطبيقات تعلّم اللغات.

---

## 1. البنية التقنية (Tech Stack)

| الطبقة | التقنية |
|---|---|
| الخادم | Django 5 + Django REST Framework |
| قاعدة البيانات | SQLite (تشغيل عبر Termux على Android) / PostgreSQL (إنتاج) |
| المصادقة | JWT عبر `djangorestframework-simplejwt` (access + refresh + تجديد تلقائي) |
| التطبيق | Flutter (Dart)، إدارة حالة عبر Riverpod، توجيه عبر go_router |
| الشبكة | Dio مع معترِض تجديد رموز تلقائي |
| الصوت | flutter_tts (نطق آلي) + audioplayers (مقاطع مسجّلة) + speech_to_text (تعرّف صوتي) |
| الوسائط | video_player + chewie لتشغيل الفيديو |
| الاستضافة | Termux على هاتف Android (خادم SQLite محلي)؛ Django يخدم `/media/` مباشرة |
| CI/CD | GitHub Actions يبني APK تلقائياً عند كل دفع وينشره في إصدار `latest-apk` |

---

## 2. هيكلية المحتوى التعليمي

```
مستوى (Level: A1 → C2، 6 مستويات CEFR)
  └── وحدة (Unit)
        └── درس (Lesson)
              └── مكوّنات مرتَّبة (Components):
                    • نص تمهيدي (text)
                    • مفردات (vocabulary) — كلمة + صورة + صوت + ترجمة + مقاطع نطقية
                    • فيديو (video) — مع ترجمة زمنية اختيارية (script)
                    • تمارين (exercise) — 8 قوالب مختلفة
```

- **180 درساً** مستوردة عبر سكربت pipeline مخصّص، موزّعة على المستويات الستة
  (A1: 20، A2: 25، B1: 30، B2: 35، C1: 40، C2: 30)، مع وسائطها الكاملة
  (صور/صوت/فيديو، ~259MB).
- استيراد المحتوى عبر أمر إدارة Django واحد (`import_content` / `import_pipeline.sh`)
  يبني الهيكل الكامل من ملفات JSON مع نسخ الوسائط تلقائياً.

### أنواع التمارين (9 قوالب)
| القالب | الوصف |
|---|---|
| `multiple_choice` | اختيار من متعدد |
| `true_false` | صحيح / خطأ بأزرار كبيرة |
| `fill_blank` | ملء الفراغ ببنك كلمات قابل للنقر |
| `matching` | مطابقة بقوائم منسدلة |
| `reorder` | إعادة ترتيب الكلمات بالسحب |
| `listening` | استماع (TTS) واختيار الإجابة |
| `dictation` | إملاء: استمع واكتب ما تسمعه |
| `pronunciation` | نطق عبر الميكروفون بتقييم متسامح |
| `final_test` | يجمع تمارين مختارة من الدرس كاختبار ختامي |

كل التصحيح يتم **في الخادم** عبر "مصحِّحات" (correctors) مخصّصة لكل قالب؛ الإجابات
الصحيحة لا تُرسَل أبداً للتطبيق قبل التحقق.

---

## 3. رحلة التعلّم متعدّدة المراحل (Micro-Learning Flow)

كل درس يُقسَّم إلى **5 مراحل** يعرضها شريط تقدّم نابض أعلى المشغّل:

1. **المقدّمة** — نص شرح (Markdown)
2. **المفردات** — بطاقات تعليمية (صورة دائرية + كلمة + صوت + ترجمة + مثال) ثم
   **اختبار فقاعات** (Bubble Quiz): صورة الكلمة + خيارات دائرية كبيرة، إجابة
   صحيحة = اهتزاز أخضر + نطق حقيقي للكلمة + انتقال تلقائي؛ خطأ = اهتزاز أحمر.
3. **الفيديو** — مقدّمة سينمائية معتمة بزر تشغيل كبير، ثم تشغيل فعلي مع:
   - ترجمة إنجليزية/عربية متزامنة (عند توفّر بيانات `script`)
   - أزرار تبديل EN/AR + التحكّم بالسرعة (0.75x / 1x / 1.25x)
4. **التمارين** — تصحيح فوري، وميض ونغمة عند الصحّ، اهتزاز + بطاقة تصحيح
   («❌ كتبت… / ✅ الصحيح…») عند الخطأ، انتقال تلقائي بعد 600ms، وزر 💡 تلميح
   (مستويين: كشف حرف/استبعاد خيارين، ثم الإجابة كاملة — بنصف النقاط).
5. **التقييم** — تمارين `final_test` المجمَّعة من الدرس.

عند إنهاء أي مرحلة يُرسَل تقدّمها للخادم (`LessonPhaseProgress`)، فتُعرض علامات
✓ **جزئية حقيقية** على مسار المستوى وشاشة خطوات الدرس (لا "الكل أو لا شيء").

### شاشة إتمام الدرس
كونفيتي + ملخّص: 🔤 عدد الكلمات المتعلَّمة، ✅ نتيجة التمارين، ⏱️ الوقت المستغرق،
🔥 السلسلة اليومية، مع نجوم (⭐⭐⭐ حسب النسبة) وزرَي «إعادة المحاولة» و«متابعة».

### إعادة الدرس
الدروس المكتملة قابلة للإعادة في أي وقت؛ ترتيب المفردات يُخلط عشوائياً لتفادي
الحفظ الآلي.

---

## 4. اختبار إتقان الوحدة (Unit Mastery Assessment)

بعد آخر درس في كل وحدة، تظهر بطاقة **اختبار الوحدة**:
- **مقدّمة**: شرح القاعدة (10 أسئلة، شرط النجاح 8/10) + خيار «ابدأ» أو «راجع الدروس».
- **الاختبار**: 10 أسئلة عشوائية من تمارين الوحدة (بواجهة نظيفة بلا تلميح ولا
  تصحيح فوري، كي يقيس الفهم الحقيقي)، تُصحَّح دفعة واحدة في الخادم.
- **النتيجة**:
  - نجاح (≥80%) → 🏆 + نجوم + تقييم 👍/👎 للوحدة + فتح الوحدة التالية.
  - رسوب → ❌ + «مراجعة الدروس» أو «إعادة الاختبار» (بلا حدّ للمحاولات).

**قاعدة الفتح التسلسلي (Mastery Gate):** الوحدة التالية تبقى مقفلة حتى تكتمل
كل دروس الوحدة الحالية **وتُجتاز اختبارها** — وليس الإكمال فقط.

---

## 5. تدريب النطق (Pronunciation Training)

ميزة تفاعلية تُقيّم نطق المتعلّم عبر ميكروفون الجهاز (`speech_to_text`)، بتساهل
مصمَّم خصيصاً للناطقين بالعربية:

- تسجيل صوتي حتى 3 محاولات لكل كلمة.
- تقييم عبر **مسافة Levenshtein موزونة** مع تسامح إضافي للأخطاء الصوتية الشائعة
  (th↔s/z، p↔b، v↔f، وغيرها) — بنفس منطق التقييم مطابَق بين الخادم والتطبيق.
- تغذية راجعة متدرّجة: ✅ ممتاز (≥90%) / ✅ جيد جداً (≥70%) / ⚠️ تقريباً (≥50%) /
  ❌ حاول مجدداً.
- من المحاولة الثانية: عرض المقاطع الصوتية قابلة للنقر (نطق منفصل لكل مقطع).
- من المحاولة الثالثة: نصيحة نطق بالعربية.
- زر «تخطَّ» بلا أي عقوبة على النقاط بعد 3 محاولات أو إن تعذّر توفّر خدمة
  التعرّف الصوتي.
- كل محاولة تُسجَّل في الخادم (`PronunciationAttempt`) لتحليل أخطاء النطق لاحقاً.

---

## 6. التحفيز والتتبّع (Gamification)

كلّه **محسوب في الخادم وقت القراءة** (بلا تكرار بيانات):
- **XP**: مجموع نقاط كل التمارين المكتملة.
- **السلسلة اليومية (Streak)**: أيام متتالية بها إكمال دروس.
- **المستوى الحالي**: أعلى مستوى قيد التقدّم أو من نتيجة اختبار تحديد المستوى.
- **بطاقة «تابع تعلّمك»**: آخر درس قيد التقدّم مع صورته المصغّرة.
- **تتبّع حالة كل كلمة مفردات**: `new → seen → learned → mastered` (إجابة صحيحة
  واحدة = "تعلّمتها"، 3 إجابات صحيحة = "أتقنتها").

---

## 7. اختبار تحديد المستوى (Placement Test)

اختبار قصير عند أول استخدام يحدّد المستوى المناسب تلقائياً (A1 إلى C2) حسب عدد
الإجابات الصحيحة، ويُسجَّل نتيجةً أولية (`PlacementResult`).

---

## 8. الشهادات (Certificates)

عند إكمال **كل دروس مستوى** بالكامل، تُصدَر شهادة تلقائياً:
- بطاقة شهادة بتصميم "ختم" مزخرف (Guilloche Seal) يحمل رمز المستوى.
- **تحميل PDF** (يُولَّد ديناميكياً عبر reportlab باسم المتعلّم ورقم شهادة فريد).
- **مشاركة** الملف مباشرة.
- **إضافة إلى LinkedIn** (رابط Add-to-Profile مع اسم الشهادة والجهة).

---

## 9. الشاشات الرئيسية

| الشاشة | الوصف |
|---|---|
| البداية (Splash) | شعار متحرّك ثم توجيه حسب حالة الجلسة |
| اختيار اللغة | عربي/إنجليزي عند أول تشغيل |
| الدخول / التسجيل | بريد + كلمة مرور، تذكّرني، أزرار Google/Apple (واجهة) |
| لوحة البداية (Home) | تحية + بطاقة المستوى الحالي + «تابع تعلّمك» + السلسلة/XP |
| مسار المستوى (Level Path) | خط زمني عمودي: ختم المستوى + بطاقة تقدّم مجزّأة + دروس بصور حقيقية مجمّعة تحت الوحدات + بطاقة اختبار كل وحدة + بطاقة الشهادة في النهاية |
| خطوات الدرس | صورة الدرس + بطاقات المراحل (فيديو/مفردات/تمارين/تقييم) بمدد تقديرية وعلامات ✓ |
| مشغّل الدرس | PageView خطوة بخطوة بكل المراحل والتمارين |
| نتيجة الدرس | كونفيتي + نجوم + ملخّص إنجاز |
| اختبار الوحدة | مقدّمة → 10 أسئلة → نتيجة (نجاح/رسوب) |
| التقدّم العام | حلقة تقدّم كبيرة + إحصائيات (دروس مكتملة/متبقية/سلسلة) + روابط الشهادات |
| الشهادات | بطاقات شهادات بتحميل/مشاركة/LinkedIn |
| الملف الشخصي | الاسم/البريد/شارة المستوى/نقاط + وصول للإعدادات |
| الإعدادات | اللغة، الوضع الداكن/الفاتح، الصوت، حول التطبيق |
| القائمة الجانبية | تنقّل سريع بين كل الأقسام + تسجيل خروج |

---

## 10. الهوية البصرية

- **تصميم فاتح افتراضياً** (مستوحى من أفضل تطبيقات تعلّم اللغات): خلفيات بيضاء
  ناصعة، ظلال ناعمة، عناوين عريضة مستديرة.
- الألوان: `#00B2E3` تركواز أساسي، `#2DCE89` أخضر النجاح، `#FF9800` برتقالي
  للحماس/XP، `#EF4444` أحمر الخطأ.
- **الوضع الداكن** متاح كخيار يحافظ على نفس الهوية بأسطح داكنة.
- ودجات مصمَّمة خصيصاً: ختم شهادة إجرائي (CustomPaint)، شريط تقدّم مجزّأ، نقاط
  خطوات، تأثيرات اهتزاز/وميض/نبض (flutter_animate)، كونفيتي عند الإنجاز.
- دعم RTL/LTR كامل مع تبديل فوري بين العربية والإنجليزية.

---

## 11. الصوت والتغذية الراجعة

- 4 نغمات قصيرة **مولَّدة برمجياً** (WAV — بلا أصول خارجية): صحيح، خطأ، إتمام
  درس، إتمام مستوى.
- اهتزاز اللمس (Haptic Feedback) مصاحب لكل نغمة.
- زر تفعيل/تعطيل الصوت في الإعدادات يُطبَّق على كل التطبيق (TTS + النغمات).

---

## 12. الأمان وسلامة البيانات

- كلمات المرور مشفَّرة، الجلسات عبر JWT مع تجديد تلقائي وحماية من الحلقات
  اللانهائية، وخروج تلقائي عند فشل التجديد.
- كل التصحيح والتقييم يحدث في الخادم؛ لا إجابات صحيحة تُكشَف للعميل قبل المحاولة.
- عزل خدمة الفيديو (`VideoService`) خلف طبقة تجريد تسمح بتبديل الاستضافة لاحقاً
  دون تغيير الواجهة.

---

## 13. النشر والتشغيل

- **الخادم**: يعمل من هاتف Android عبر Termux (Python + SQLite)، مع
  `termux_setup.sh` لإعداد البيئة بأمر واحد، و`termux-wake-lock` لإبقائه نشطاً.
- **التطبيق**: يُبنى تلقائياً (APK) عبر GitHub Actions عند كل دفع، ويُنشَر في
  إصدار GitHub باسم ثابت `latest-apk` لتنزيل مباشر ومستقر دون الحاجة لمتجر
  تطبيقات.
- عنوان الخادم يُضبَط وقت البناء (`--dart-define=API_BASE_URL=...`)، افتراضياً
  `127.0.0.1:8000` (الخادم والتطبيق على نفس الهاتف).

---

## 14. الاختبارات الآلية

مجموعة اختبارات pytest شاملة للخادم تغطي: المصادقة، هيكل المحتوى، مصحِّحات
التمارين كافّة، اختبار الوحدة (توليد/تصحيح/بوابة الفتح)، تتبّع المفردات، مراحل
الدرس، التلميحات وكشف الإجابات، الشهادات، النطق المتسامح — أكثر من 57 اختباراً
ناجحاً، تُشغَّل تلقائياً قبل كل دفع.

---

*هذا الوصف يعكس الحالة الحالية للمستودع على فرع
`claude/code-master-prompt-t3cu6t`.*

# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
# Format: postgres://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=postgres://lms:lms@localhost:5432/lms

# JWT lifetimes (minutes / days)
JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7

# CORS (comma-separated origins; empty = allow all in DEBUG)
CORS_ALLOWED_ORIGINS=

# Google Sign-In (v2 §6) — Android + Web OAuth client IDs; verified server-side
GOOGLE_CLIENT_ID=
GOOGLE_WEB_CLIENT_ID=
APPLE_CLIENT_ID=

# Media service (hosting decision deferred — see master prompt §7).
# Must end at /media/ : storage keys carry their own kind prefix
# (videos/…, images/…, audio/…). Deployed value: http://84.8.223.62/media/
VIDEO_PLAYBACK_BASE_URL=http://localhost:8000/media/

# Upload ceilings per kind, in MB (content media upload API)
MEDIA_MAX_VIDEO_MB=200
MEDIA_MAX_IMAGE_MB=10
MEDIA_MAX_AUDIO_MB=25

# --- v2 expansion ------------------------------------------------------- #
# Coins economy (v2 §2.3)
COINS_PER_CORRECT_ANSWER=5
COINS_COMPLETION_BONUS=10
COINS_DAILY_CAP=60

# AI providers (v2 §2.2 news pipeline + §3 AI Tutor)
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-5
OPENAI_API_KEY=
GOOGLE_TTS_API_KEY=
GOOGLE_TTS_VOICE=en-US-Neural2-F

# AI Tutor daily caps (v2 §3.5)
TUTOR_FREE_DAILY_SESSIONS=2
TUTOR_PREMIUM_DAILY_SESSIONS=10
TUTOR_MAX_EXCHANGES=6

# News source (v2 §2.2)
NEWSAPI_KEY=
NEWS_COUNTRY=ma

# Payments (v2 §7)
ANDROID_PACKAGE_NAME=
GOOGLE_PLAY_SERVICE_ACCOUNT_JSON=
PLAY_PRODUCT_MONTHLY=premium_monthly
PLAY_PRODUCT_ANNUAL=premium_annual
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_ANNUAL=
CMI_MERCHANT_ID=
CMI_STORE_KEY=
CMI_GATEWAY_URL=https://payment.cmi.co.ma/fim/est3Dgate

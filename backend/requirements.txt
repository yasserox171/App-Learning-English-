# Core
Django==5.0.6
djangorestframework==3.15.1
psycopg[binary]==3.1.19

# Config / env
django-environ==0.11.2

# Time zone database (needed on minimal systems without system tz files)
tzdata==2024.1

# Auth
djangorestframework-simplejwt==5.3.1
PyJWT==2.8.0

# API utilities
django-cors-headers==4.3.1
django-filter==24.2
drf-spectacular==0.27.2

# Certificates (PDF): rendered by the dependency-free generator in
# apps/progress/certificate_pdf.py — no PDF library needed.

# Dev / testing
pytest==8.2.1
pytest-django==4.8.0
gunicorn==22.0.0

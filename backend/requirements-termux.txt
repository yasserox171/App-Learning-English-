# Termux / on-phone setup — uses SQLite, no PostgreSQL driver needed.
# (reportlab is omitted; it's only needed for certificate PDFs and is
#  imported lazily, so everything else works without it.)

Django==5.0.6
djangorestframework==3.15.1

# Config / env
django-environ==0.11.2

# Auth
djangorestframework-simplejwt==5.3.1
PyJWT==2.8.0

# API utilities
django-cors-headers==4.3.1
django-filter==24.2

# NOTE: drf-spectacular (API docs) is intentionally omitted — it pulls in
# rpds-py which needs Rust to build on Termux. The app runs fine without it;
# only the /api/v1/docs schema UI is unavailable. Settings/urls handle this.
